# Audio panel opened by clicking the volume module: output and input levels,
# device choice and per-application sources. Polls every 200 ms while open.
attach = "volume"
provider = "builtin:volume"
interval = "1m"
focusable = true
popup = { width = 380, height = 640 }

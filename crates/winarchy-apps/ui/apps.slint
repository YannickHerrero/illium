// Single compilation unit: every window shares one Palette global.
import { Palette } from "common.slint";
import { TasksWindow, TaskRow } from "tasks.slint";
export { Palette, TasksWindow, TaskRow }
